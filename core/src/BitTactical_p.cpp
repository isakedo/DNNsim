
#include <core/BitTactical_p.h>

namespace core {

    /* AUXILIARY FUNCTIONS */

    /* CYCLES */

    template <typename T>
    void BitTactical_p<T>::computeConvolution(const core::Layer<T> &layer, sys::Statistics::Stats &stats) {

    }

    template <typename T>
    void BitTactical_p<T>::computeInnerProduct(const Layer<T> &layer, sys::Statistics::Stats &stats) {

    }

    template <typename T>
    void BitTactical_p<T>::run(const Network<T> &network) {

    }

    /* POTENTIALS */

    template <typename T>
    void BitTactical_p<T>::computePotentialsConvolution(const core::Layer<T> &layer, sys::Statistics::Stats &stats) {

    }

    template <typename T>
    void BitTactical_p<T>::computePotentialsInnerProduct(const Layer<T> &layer, sys::Statistics::Stats &stats) {

    }

    template <typename T>
    void BitTactical_p<T>::potentials(const Network<T> &network) {

    }

    template class BitTactical_p<uint16_t>;

}

