
#include <core/BitTactical_e.h>

namespace core {

    /* AUXILIARY FUNCTIONS */

    /* CYCLES */

    template <typename T>
    void BitTactical_e<T>::computeConvolution(const core::Layer<T> &layer, sys::Statistics::Stats &stats) {

    }

    template <typename T>
    void BitTactical_e<T>::computeInnerProduct(const Layer<T> &layer, sys::Statistics::Stats &stats) {

    }

    template <typename T>
    void BitTactical_e<T>::run(const Network<T> &network) {

    }

    /* POTENTIALS */

    template <typename T>
    void BitTactical_e<T>::computePotentialsConvolution(const core::Layer<T> &layer, sys::Statistics::Stats &stats) {

    }

    template <typename T>
    void BitTactical_e<T>::computePotentialsInnerProduct(const Layer<T> &layer, sys::Statistics::Stats &stats) {

    }

    template <typename T>
    void BitTactical_e<T>::potentials(const Network<T> &network) {

    }

    template class BitTactical_e<uint16_t>;

}