
#include <core/BitFusion.h>

namespace core {

    template <typename T>
    void BitFusion<T>::computeConvolution(const core::Layer<T> &layer, sys::Statistics::Stats &stats) {

    }

    template <typename T>
    void BitFusion<T>::run(const Network<T> &network) {

    }

    template class BitFusion<uint16_t>;

}